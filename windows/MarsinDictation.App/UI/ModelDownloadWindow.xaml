<Window x:Class="MarsinDictation.App.UI.ModelDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading Model" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Initial Setup" FontWeight="Bold" FontSize="16" Grid.Row="0" Margin="0,0,0,10"/>
        
        <TextBlock x:Name="MessageText" Text="MarsinDictation needs to download the local speech model (ggml-large-v3-turbo-q5_0.bin) for offline transcription." TextWrapping="Wrap" Grid.Row="1" Margin="0,0,0,10"/>
        
        <ProgressBar x:Name="DownloadProgress" Minimum="0" Maximum="100" Height="20" Grid.Row="2" Margin="0,0,0,5" />
        
        <TextBlock x:Name="ProgressText" Text="Waiting..." HorizontalAlignment="Center" Grid.Row="3" Margin="0,0,0,15" Foreground="Gray"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SkipButton" Content="Skip (Use Cloud)" Width="120" Height="30" Margin="0,0,10,0" Click="OnSkipClicked" />
            <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="30" Click="OnCancelClicked" />
        </StackPanel>
    </Grid>
</Window>
