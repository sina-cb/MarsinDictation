# Project: Windows App v0

**Created:** 2026-03-13  
**Status:** ✅ Phases 0–4 Complete (core pipeline functional)  
**Design Reference:** [Windows v0 Design](../../01_designs/01_win_design_v0.md)  
**Branch:** `dev/win_v0` (local, not pushed)

### Related Documents

Read these before starting implementation:

| Document | Why |
|----------|-----|
| [Codex](../../00_gol/00_codex.md) | Contribution rules, coding standards, validation expectations |
| [Privacy & Security](../../00_gol/01_privacy.md) | Audio handling, API key storage, data retention rules |
| [Git Workflow](../../00_gol/02_git.md) | Branch strategy (`dev/win_v0`), squash merge into `main` |
| [Build & Deploy](../../00_gol/03_build_and_deploy.md) | `deploy.py` design, Windows build steps |
| [Testing Framework](../../00_gol/04_agent_driven_test.md) | Evidence-based testing framework, coverage map |
| [Temp Files](../../00_gol/05_temp_files.md) | Temporary file rules (`tmp/` directory) |
| [Utilities](../../01_designs/03_utils.md) | `util/` directory — recorder and dev tools |
| [Metrics Design](../../01_designs/04_metrics.md) | JSONL metrics ledger in `.metrics/` |
| [Aspirational Design](../../01_designs/00_aspirational_deisgn.md) | Long-term cross-platform architecture context |

---

## Goal

Implement the Windows v0 dictation app as defined in the Windows v0 design document.

---

## Dictation State Model

The service must operate on a well-defined state machine from Phase 0 onward:

```
Idle → Recording → Transcribing → ReadyToInject → Idle
                                 → PendingRecovery → Idle
                                 → Error → Idle
```

| State | Meaning |
|-------|---------|
| `Idle` | Waiting for hotkey |
| `Recording` | WASAPI capture active, overlay visible |
| `Transcribing` | Audio sent to provider, awaiting response |
| `ReadyToInject` | Text received, injection ladder executing |
| `PendingRecovery` | Injection failed, transcript stored, awaiting Alt+Shift+Z |
| `Error` | Transient failure (no mic, API unreachable), auto-returns to Idle |

---

## Phases

### Phase 0 — Bootstrap & Walking Skeleton ✅

De-risk the annoying parts first: WPF shell, tray integration, hotkey plumbing, app lifetime, and local settings/security direction.

- [x] Create .NET 8 WPF solution and project structure
- [x] Confirm app launches cleanly on target dev machine
- [x] Add H.NotifyIcon.Wpf and verify tray icon appears
- [x] Verify app can start hidden / minimize to tray
- [x] Add HotkeyManager with WH_KEYBOARD_LL hook — Ctrl+Win hold-to-record confirmed working
- [x] Verify hotkeys unregister cleanly on shutdown
- [x] Add notification mechanism for placeholder actions (StatusWindow overlay)
- [x] Define settings storage location (`%LOCALAPPDATA%/MarsinDictation/settings.json`) and DPAPI secret storage (`SecretStore.cs`)
- [x] Add transcript state model (`TranscriptEntry`, `TranscriptState`) and storage (`TranscriptStore.cs`)
- [x] Add basic logging policy and gitignored temp/output location (`tmp/`)
- [x] Confirm `dotnet restore`, `dotnet build`, and `dotnet run` work from a clean checkout
- [x] `deploy.py` — OS detection, `--build`, `--run`, `--test`, `--hold` modes with evidence output

#### Phase 0 Test Evidence ✅

39 evidence tests verify Phase 0 infrastructure. See [Testing Framework](../../00_gol/04_agent_driven_test.md) for the full spec.

| Suite | Tests | What It Covers |
|-------|-------|----------------|
| `DictationStateTests` | 18 | State machine transitions, toggle traces, invariants, event firing |
| `SettingsManagerTests` | 6 | Design-doc defaults, persistence, corrupt/empty JSON fallback, secret exclusion |
| `TranscriptStoreTests` | 9 | CRUD, ordering, state filtering, recovery priority, persistence, privacy wipe, corrupt fallback |
| `TranscriptionTests` | 3 | OpenAI real transcription, TranscriptionResult success/failure |
| `EnvLoaderTests` | 3 | .env parsing, KEY=VALUE extraction |

Run: `python devtool/deploy.py --test` (compact) or `--test --verbose` (full evidence)

---

### Phase 1 — Core App Infrastructure ✅

Wire up the real recording pipeline with live audio, real transcription calls, and the recovery path.

- [x] App state machine (`DictationService.cs` — Idle / Recording / Transcribing / ReadyToInject / PendingRecovery / Error)
- [x] TranscriptStore with state tracking (`TranscriptStore.cs` — success / pending / failed_transcription)
- [x] Real HotkeyManager wiring: Ctrl+Win hold starts recording, release stops + transcribes
- [x] Notification plumbing — toast infrastructure for status/failure messages
- [x] Wire StatusWindow overlay to show recording state
- [x] Recovery hotkey wired to transcript state (re-injects `_lastTranscription`)

### Phase 2 — Audio Capture ✅

- [x] WASAPI microphone capture (`AudioRecorder.cs` — real WASAPI loopback via NAudio)
- [x] WAV encoding (NAudio `WaveFileWriter` direct to `MemoryStream`)
- [ ] AudioGuard (25 MB upload-size limit — auto-stop before exceeding)

### Phase 3 — Transcription ✅

- [x] `ITranscriptionClient` interface
- [x] `OpenAITranscriptionClient` (multipart/form-data upload to `/v1/audio/transcriptions`)
- [x] `LocalAITranscriptionClient` (uses `OpenAITranscriptionClient` with configurable base URL and no auth)
- [x] Model selection support (via `.env`: `OPENAI_MODEL`)
- [x] Live transcription confirmed working (audio → OpenAI → text → inject → TranscriptStore)
- [~] Secure API key retrieval from `SecretStore` (DPAPI) — **skipped for now** (using `.env`)

### Phase 4 — Injection ✅

- [x] `SendInputInjector` (Unicode `KEYEVENTF_UNICODE` keystrokes — primary method)
- [x] Injection from transcription result (wired in `App.xaml.cs` → `DoInjectText()`)
- [x] Injection failure → mark transcript as "pending" + clipboard fallback + toast notification
- [ ] Per-app exclusion list enforcement (check at capture-start and recovery)
- [x] Recovery hotkey wired to transcript state (pop pending, re-run injection)


### Phase 6 — Validation

- [ ] End-to-end in: Notepad, Chrome textarea, Slack, Word, VS Code
- [ ] Both providers tested (OpenAI + LocalAI)
- [ ] Failure modes validated (no mic, API down, injection blocked, 25 MB limit)
- [ ] Recovery path validated (Alt+Shift+Z with pending transcripts)
- [ ] System clipboard is never modified by the app

---

## Dev Tools & Logging

| Tool | Purpose |
|------|---------|
| `deploy.py --run --hold` | Launch app + tail colorized logs |
| `deploy.py --test --filter X` | Run specific tests with verbose evidence |
| `util/record.py` | Standalone audio recorder for test fixtures |
| `tmp/logs/deploy.log` | Deploy script log (Python `logging`) |
| `tmp/logs/app.log` | Mirrored app log (from `--hold` tailing) |
| `%LOCALAPPDATA%/MarsinDictation/logs/app.log` | App log (written by `FileLoggerProvider`) |

---

## Notes

- Each phase should be completable independently with testable outcomes.
- Phase 0 exists specifically to prevent setup ambiguity from blocking real feature work.
- Recovery hotkey belongs to core infrastructure (Phase 1), not polish.
- Toast notifications belong to core infrastructure (Phase 1), not UI polish.
- Transcript storage model must exist before the history UI panel (Phase 5).
- This plan will be updated as implementation progresses.
